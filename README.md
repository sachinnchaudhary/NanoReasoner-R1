# NanoReasoner-R1

Minimal-compute RLVR/GRPO experiments with verifiable rewards.

## Layout
- configs/    experiment config
- scripts/    entrypoints
- src/rlvr/   core utilities (prompts, verifiers, mixture)
- data/, outputs/, checkpoints/ are local-only (gitignored)

## Setup
pip install -r requirements.txt
